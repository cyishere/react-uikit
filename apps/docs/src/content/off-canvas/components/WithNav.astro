---
import PreviewSection from '@/components/PreviewSection.astro';
import { extractComponentCode } from '@/utils/code';

import WithNavReact from './WithNav.tsx';
import rawCode from './WithNav.tsx?raw';

const codeSnippet = extractComponentCode(rawCode);
---

<PreviewSection codeSnippet={codeSnippet} lang="jsx">
  <WithNavReact client:only="react" />
</PreviewSection>
