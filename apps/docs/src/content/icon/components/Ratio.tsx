import { Icon } from 'react-uikit';

const BasicUsage = () => {
  return (
    <div className="uk-flex uk-flex-middle">
      <Icon name="check" ratio={2} className="uk-margin-small-right" />
      <Icon name="check" ratio={3} />
    </div>
  );
};

export default BasicUsage;
