---
title: Grid
description: Create a fully responsive, fluid and nestable grid layout.
---

import BasicUsage from '@/components/Grid/BasicUsage.astro';
import GapModifier from '@/components/Grid/GapModifier.astro';
import NestedGrid from '@/components/Grid/NestedGrid.astro';
import DividerModifier from '@/components/Grid/DividerModifier.astro';
import MatchHeight from '@/components/Grid/MatchHeight.astro';
import Warning from '@/components/Warning.astro';
import GridWithWidth from '@/components/Grid/GridWithWidth.astro';
import ChildWidth from '@/components/Grid/ChildWidth.astro';
import GridAndFlex from '@/components/Grid/GridAndFlex.astro';

## Usage

Coming soon.

<BasicUsage />

### Gap Modifier

For modifying the gap, use the needed UIkit CSS classes, just like the og UIkit.

<GapModifier />

### Nested Grid

You can easily extend your grid layout with nested grids.

```jsx
<Grid>
  <div>
    <Grid>
      <div></div>
      <div></div>
    </Grid>
  </div>
  <div>
    <Grid>
      <div></div>
      <div></div>
    </Grid>
  </div>
</Grid>
```

<NestedGrid />

### Divider Modifier

Same as og UIkit, add the `.uk-grid-divider` class to separate grid cells with lines.

```jsx
<Grid className="uk-grid-divider"></Grid>
```

<DividerModifier />

## Match Height

In `react-uikit`, for matching the height of grid cells, we need to apply `matchHeight` prop to `Grid`, by default height match apply to the direct children of `Grid`.

```jsx
<Grid matchHeight></Grid>
```

We can target explicit children by applying the target CSS class to `matchHeight`.

<Warning>
  Don't forget to put the dot `.` in front of the class name — `.uk-card-body`.
</Warning>

```jsx
<Grid matchHeight=".uk-card-body"></Grid>
```

<MatchHeight />

## Grid with Width

As in the original UIkit, `Grid` is also used in combination with the [Width component](https://getuikit.com/docs/width).

```jsx
<Grid>
  <div className="uk-width-auto@m"></div>
  <div className="uk-width-1-3@m"></div>
  <div className="uk-width-expand@m"></div>
</Grid>
```

<GridWithWidth />

### Child Width

If the grid columns are evenly split, you can add one of the `.uk-child-width-*` classes to `Grid` instead of adding a class to each of the items.

```jsx
<Grid className="uk-child-width-1-2@s uk-child-width-1-3@m"></Grid>
```

<ChildWidth />

## Grid and Flex

You can easily combine the `Grid` with the [Flex component](https://getuikit.com/docs/flex) by using the `.uk-flex-*` classes.

<GridAndFlex />